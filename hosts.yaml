---
control:
    hosts:
        local-control:
            ansible_connection: local
        control-vm:
            ansible_host: control

app_servers:
    hosts:
        jenkins-server-vm:
            ansible_host: appserver
        gcloud-frontend-server:
            ansible_host: devops-f-vm
        gcloud-backend-server:
            ansible_host: devops-b-vm

postgres:
    hosts:
        gcloud-db-vm:
            ansible_host: devops-db-vm
        jenkins-db-vm:
            ansible_host: appserver